import SwiftUI
import DiskGalleryCore

/// Modern two-pane workspace router (spec §4.3).
/// NOTE: stub — wraps the existing content router so the shell compiles;
/// the volume bento (Task 11) and route shells (Task 12) replace this.
struct ModernWorkspace: View {
    var body: some View {
        ContentColumn()
    }
}
