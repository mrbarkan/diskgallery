import SwiftUI
import DiskGalleryCore

/// Modern glass sidebar (spec §6).
/// NOTE: stub — wraps the existing sidebar so the shell compiles; rebuilt in Task 5.
struct ModernSidebar: View {
    var body: some View {
        LibrarySidebarView()
    }
}
